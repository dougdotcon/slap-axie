function shopSetup() {
  // Inicialização e configuração dos elementos da tela de loja, como botões e elementos da interface
}

function drawShop() {
  textAlign(CENTER, CENTER);
  textSize(32);
  text("Loja", width / 2, height / 4);

  // Exibição de elementos adicionais da interface da Loja, como lista de pacotes de cartas e itens cosméticos disponíveis
}

function shopMousePressed() {
  // Lógica para interagir com itens na tela de Loja ao clicar no mouse, como comprar pacotes de cartas ou itens cosméticos
}