function drawCollection() {
  textAlign(CENTER, CENTER);
  textSize(32);
  text("Coleção e Criação de Decks", width / 2, height / 4);
}

function collectionSetup() {
  // Adicione aqui qualquer configuração específica da tela de coleção
}

function collectionMousePressed() {
  // Adicione aqui qualquer lógica de evento de clique do mouse específica da tela de coleção
}